# Example pipeline applying Illumina sequencing
# followed by the new decay channel to simulate long-term storage
input: encoded/message.fasta
output: decay_output.fasta

simulators:
  - illumina

decay:
  half_life: 1000   # days
  variation: 0.1

pipeline:
  illumina_profile: hiseq

